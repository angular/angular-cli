import {Provider, PLATFORM_PIPES} from 'angular2/core';

// import {NodeAsyncPipe} from './node-async-pipe';


export const NODE_PIPES: Array<any> = [
  // NodeAsyncPipe
];

export const NODE_PLATFORM_PIPES: Array<Provider> = [
  // new Provider(PLATFORM_PIPES, {useValue: NODE_PIPES, multi: true})
];
