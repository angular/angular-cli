import { <%= classify(name) %><%= !noSuffix ? 'Pipe' : '' %> } from './<%= dasherize(name) %><%= !noSuffix ? '.pipe' : '' %>';

describe('<%= classify(name) %><%= !noSuffix ? 'Pipe' : '' %>', () => {
  it('create an instance', () => {
    const pipe = new <%= classify(name) %><%= !noSuffix ? 'Pipe' : '' %>();
    expect(pipe).toBeTruthy();
  });
});
