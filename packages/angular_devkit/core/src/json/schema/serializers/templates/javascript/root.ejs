<%
const extras = {
  exceptions,
  options,
  strings,
  symbols,
  templates,
};
%>

const holder = <%= templates.subschema(Object.assign({
  name: name,
  path: '',
  required: false,
  schema: schema,
}, extras))
%>;

holder.set(value);
return holder.get();
